# Registry module

Creates immutable, encrypted ECR repositories for the web, API and ingestion worker. Images are scanned on push and bounded by a ten-image lifecycle policy for this short-lived lab.
